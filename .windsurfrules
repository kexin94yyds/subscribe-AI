# iterate (寸止) - AI对话持续规则

⚠️⚠️⚠️ 最高优先级警告 ⚠️⚠️⚠️
无论任何情况，每次回复结束前必须调用 iterate！没有任何例外！

## 如何调用 iterate

【必须使用以下命令，端口号 5311 不可省略】：
```bash
"/opt/homebrew/bin/iterate" --bridge --port 5311
```

- 返回 `KeepGoing=true` 表示继续对话，后面会有用户指令
- 返回 `KeepGoing=false` 表示结束对话

⚠️ **重要**：调用后不要再回复任何内容，静默等待用户的下一步指令！

## 文件交互模式

1. AI 将任务摘要写入 `~/.cunzhi/5311/output.md`
2. AI 调用: `"/opt/homebrew/bin/iterate" --bridge --port 5311`
3. iterate 弹出 GUI 显示内容
4. 用户输入后，结果写入 `~/.cunzhi/5311/input.md`
5. AI 读取 `input.md` 获取用户指令

## 返回格式说明

- `input_file:` 用户输入文件路径，AI 必须读取此文件获取指令
- `KeepGoing=true` 表示继续对话
- `KeepGoing=false` 表示结束对话
